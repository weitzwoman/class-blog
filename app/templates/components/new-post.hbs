{{#if addNewPost}}
  <h2>New Post</h2>
  <form>
    <div class="input-field">
      {{input value=title id="title" placeholder="Post Title"}}
    </div>
    <div class="input-field">
      {{input value=date id="date" type="date" placeholder="Date"}}
    </div>
    <div class="input-field">
      {{input value=photo id="photo" placeholder="Add Photo"}}
    </div>
    <div class="input-field">
      {{input value=content id="content" placeholder="Description of Route and Trip"}}
    </div>
    <button class="btn" {{action 'savePost'}}>Add Trip Report</button>
  </form>
{{else}}
  <br>
  <button class="btn-large" {{action 'postFormShow'}}>Add New Blog Post</button>
{{/if}}
